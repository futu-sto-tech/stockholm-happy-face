export type UserImage = {
  id: string;
  url: string;
  link: string;
  week: number;
  year: number;
};

export type GifImage = {
  id: string;
  url: string;
  originalUrl: string;
  link: string;
};
