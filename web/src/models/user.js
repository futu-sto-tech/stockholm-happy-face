export default class User {
  constructor(name, email, avatar, nickname) {
    this.id = null;
    this.name = name;
    this.nickname = nickname;
    this.email = email;
    this.avatar = avatar;
  }
}
